{
  "setup": "npm install --package-lock=false --prefer-offline --no-audit",
  "test": "npx tsc --noEmit",
  "build": "npm run build",
  "deploy_mode": "live_checkout",
  "live_branch": "main",
  "deploy": "npm run build && bash scripts/aibox-deploy.sh"
}
